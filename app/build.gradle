apply plugin: 'com.android.application'

android {
//    signingConfigs {
//        config {
//            try {
//                def Properties keyFile = new Properties()
//                keyFile.load(new FileInputStream(file('../local.properties')))
//                keyAlias 'baby'
//                keyPassword 'baby123456'
//                storeFile file("${keyFile['keyFile']}")
//                storePassword 'baby123456'
//            } catch (ignore) {
//                throw new InvalidUserDataException("You should define keyFile path in root local.properties.")
//            }
//        }
//    }
    compileSdkVersion 22
    buildToolsVersion '23.0.1'
    defaultConfig {
        applicationId "com.yikang.health"
        minSdkVersion 15
        targetSdkVersion 22
        versionCode 1
        versionName "1.0.0.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
//            signingConfig signingConfigs.config
        }
//        debug {
//            signingConfig signingConfigs.config
//        }
    }
    sourceSets {
        main {
            jniLibs.srcDirs = ['libs']
        }
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    testCompile 'junit:junit:4.12'
    compile 'com.android.support:support-v4:24.0.0-alpha1'
    //    compile 'com.zhy:autolayout:1.4.3'
    //    compile project(':library')
    compile files('libs/uil.jar')
}
